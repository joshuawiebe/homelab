# ===========================
# Vaultwarden Environment
# ===========================
# Admin token (hashed!) – used to access the admin interface.
# Generate with:
#   docker run --rm -it vaultwarden/server /vaultwarden hash
#
# Then copy the full output (starting with `$argon2id$v=19$...`) here ↓
ADMIN_TOKEN=(paste hashed admin password here)
